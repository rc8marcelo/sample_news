import 'package:flutter/material.dart';

class NewsListError extends StatelessWidget {
  const NewsListError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
