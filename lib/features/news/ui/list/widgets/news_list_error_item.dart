import 'package:flutter/material.dart';

class NewsListErrorItem extends StatelessWidget {
  const NewsListErrorItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
